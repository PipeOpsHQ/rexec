# Rexec Production Environment Configuration
# Copy this file to .env and fill in the values

# ===========================================
# REQUIRED - Security
# ===========================================

# JWT secret for authentication (generate with: openssl rand -hex 32)
JWT_SECRET=your-secure-jwt-secret-here

# PostgreSQL password (generate with: openssl rand -hex 16)
POSTGRES_PASSWORD=your-secure-postgres-password-here

# ===========================================
# REQUIRED - Stripe Billing
# ===========================================

# Stripe API secret key (from https://dashboard.stripe.com/apikeys)
STRIPE_SECRET_KEY=sk_live_your_stripe_secret_key

# Stripe webhook signing secret (from webhook endpoint settings)
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Stripe Price IDs for subscription tiers (from Products page)
STRIPE_PRICE_PRO=price_your_pro_tier_price_id
STRIPE_PRICE_ENTERPRISE=price_your_enterprise_tier_price_id

# ===========================================
# REQUIRED - Domain Configuration
# ===========================================

# Your domain (used for SSL and Stripe redirects)
DOMAIN=example.com
BASE_URL=https://example.com

# Email for Let's Encrypt SSL certificate notifications
LETSENCRYPT_EMAIL=admin@example.com

# ===========================================
# OPTIONAL - Resource Limits
# ===========================================

# Maximum containers per tier (defaults shown)
# MAX_CONTAINERS_FREE=2
# MAX_CONTAINERS_PRO=5
# MAX_CONTAINERS_ENTERPRISE=20

# Container idle timeout in minutes (default: 30)
# IDLE_TIMEOUT_MINUTES=30

# ===========================================
# OPTIONAL - Redis Configuration
# ===========================================

# Redis URL (default uses docker compose service)
# REDIS_URL=redis://redis:6379

# ===========================================
# OPTIONAL - Logging
# ===========================================

# Log level: debug, info, warn, error (default: info)
# LOG_LEVEL=info

# GIN mode: debug, release (default: release in production)
# GIN_MODE=release
